---
title: 简介
description: 低代码时代的元数据驱动文档引擎。
---

import { FileJson, Layers, Cpu, Zap } from 'lucide-react';

# 什么是 ObjectDocs?

**ObjectDocs** 是一个基于 **Next.js 14** 和 **Fumadocs** 构建的现代文档生成器。

它的诞生是为了解决 **ObjectStack** 生态系统中的一个特定问题：*如何记录复杂的多产品低代码平台，而又不陷入繁琐的前端维护？*

与通用的静态站点生成器 (SSG) 不同，ObjectDocs 将文档结构视为 **数据**，而不是代码。

## 核心理念

<Cards>
  <Card
    icon={<FileJson />}
    title="元数据驱动"
    href="/docs/getting-started/configuration"
  >
    完全通过本地 `meta.json` 文件控制侧边栏、导航和页面排序。内容更新无需 React 知识。
  </Card>
  <Card
    icon={<Cpu />}
    title="低代码原生支持"
    href="/docs/components/objectui-embed"
  >
    直接在 Markdown 中嵌入 JSON 模式。渲染由 ObjectQL 定义驱动的实时交互组件。
  </Card>
</Cards>

## 为什么选择 ObjectDocs?

### 1. 关注点分离
在传统的文档站点中，添加新章节通常需要编辑全局 `sidebar.js` 配置或修改 React 组件。在 ObjectDocs 中，我们严格区分角色：

* **技术文档工程师**: 编写 `.mdx` 内容并管理本地 `meta.json` 进行排序。
* **平台架构师**: 配置全局 `content/docs.site.json` (品牌, 导航链接)。
* **前端开发人员**: 站点引擎作为独立包 (`@objectdocs/site`) 维护，隔离在 `content/.fumadocs` 中。

### 2. 整洁集成
ObjectDocs 设计为可添加到任何现有项目：
* 所有文档都在 `content/` 目录中
* 站点引擎复制到 `content/.fumadocs` (gitignored)
* 你的项目根目录保持整洁
* 仓库中没有散落的配置文件

### 3. 交互式组件
静态文本不足以展示低代码平台的能力。ObjectDocs 允许你在文档中直接挂载实时的组件演示。

## 下一步

准备好构建你的文档了吗？

<Cards>
<Card title="快速开始" href="/docs/getting-started" icon={<Zap />}>
在 5 分钟内安装 ObjectDocs 并创建你的第一个页面。
</Card>
<Card title="架构详解" href="/docs/getting-started/architecture" icon={<Layers />}>
深入了解目录结构和数据流。
</Card>
</Cards>
